spring:
  profiles:
    active:
    - dev
---
spring:
  profile: dev
  application:
    name: microservicecloud-config-client-dev

---
spring:
  profiles: test
  application:
    name:  microservicecloud-config-client-test

